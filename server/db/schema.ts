// Schema definitions for reference only
// In serverless mode, we don't use local database schema

export const jobs = null;
export const clients = null;
export const servers = null;
export const invoices = null;
export const contacts = null;
export const sync_log = null;

console.log('Schema module loaded in serverless mode (no local database)');
