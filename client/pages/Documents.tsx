import PlaceholderPage from "./PlaceholderPage";
import { Scale } from "lucide-react";

export default function Documents() {
  return (
    <PlaceholderPage
      title="Document Management"
      description="Manage legal documents to be served, attachments, affidavits, and automatic document analysis with embedded PDF viewer."
      icon={Scale}
    />
  );
}
