import PlaceholderPage from "./PlaceholderPage";
import { FileText } from "lucide-react";

export default function Jobs() {
  return (
    <PlaceholderPage
      title="Job Management"
      description="Comprehensive job tracking, status management, and service coordination with ServeManager integration."
      icon={FileText}
    />
  );
}
